# Dependencies for the AgentCore Runtime container (PRD 30).
#
# The AgentCore starter toolkit builds a container from this file plus the repo
# source, and runs agents/agentcore_app.py inside it. It is deliberately smaller
# than the full project: the runtime executes the workflow, so it needs the
# domain, tools and agent packages - but not FastAPI, uvicorn or the test stack.
#
#   agentcore configure -e agents/agentcore_app.py -r us-east-1 \
#       --requirements-file requirements-agentcore.txt

pydantic>=2.7
boto3>=1.34

# Agent runtime
strands-agents>=1.0
bedrock-agentcore>=1.0

# Traces and metrics in the AgentCore console. The toolkit expects this to be
# present for observability to work.
aws-opentelemetry-distro
