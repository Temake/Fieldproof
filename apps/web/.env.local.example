# Where the web server reaches the FieldProof API. Server-side only.
FIELDPROOF_API_URL=http://localhost:8000

# Must match the API's FIELDPROOF_API_KEY when that is set. Server-side only:
# the browser never sees it - app/api/[...path]/route.ts adds it to requests.
FIELDPROOF_API_KEY=
